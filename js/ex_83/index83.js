/* let astr = '';
 let line = -1;
 while (astr>0 && astr==13) {
     astr='*'
     astr.length*line

     console.log(astr)
 } */

 let astr = '*';
 let line = -1;
 do {
     astr.length*line;
     console.log(astr);
 } while(astr>0 && astr<=13)