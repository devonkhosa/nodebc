/* let astr = '';
 let line = -1;
 while (astr>0 && astr==13) {
     astr='*'
     astr.length*line

     console.log(astr)
 } */

/*  let astr = '*';
 let line = -1;
 do {
     astr.length*line;
     console.log(astr);
 } while(astr>0 && astr<=13) */

 for (let astr='*'&&let line =-1; astr.length >0 && ast.lengthr<=13; astr.length*line) {
     console.log(astr)
 }